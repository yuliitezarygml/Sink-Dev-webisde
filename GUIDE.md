# Photography Portfolio Website 📸

Красивый портфолио сайт для фотографа в тёмном стиле с модульной архитектурой.

## 📁 Структура проекта

```
src/
├── components/
│   ├── Hero/              # Главный герой (приветственный экран)
│   │   ├── Hero.tsx       # React компонент
│   │   └── Hero.css       # Стили (изолированные)
│   │
│   ├── Gallery/           # Галерея фото
│   │   ├── Gallery.tsx    # React компонент
│   │   └── Gallery.css    # Стили (изолированные)
│   │
│   ├── About/             # Информация о фотографе
│   │   ├── About.tsx      # React компонент
│   │   └── About.css      # Стили (изолированные)
│   │
│   └── Contact/           # Форма контакта
│       ├── Contact.tsx    # React компонент
│       └── Contact.css    # Стили (изолированные)
│
├── App.tsx               # Главный компонент приложения
├── App.css               # Глобальные стили
├── index.tsx             # Точка входа
├── index.css             # Глобальные CSS переменные
└── ...остальные файлы
```

## 🎨 Компоненты

### 1. **Hero** - Главный экран
- Полноэкранный фон с фото
- Показывает имена молодожёнов, дату
- Информация о фотографе и ссылка на студию
- Анимированная стрелка вниз

**Редактируемые параметры в `Hero.tsx`:**
```tsx
<Hero 
  date="13.10.2025"
  names={{ first: 'Trofim', second: 'și Daniela' }}
  photographer="Ion Ghiletchi"
  studio="ghiframe.studio"
  studioLink="https://ghiframe.studio"
/>
```

### 2. **Gallery** - Галерея фото
- Адаптивная сетка фото
- Модальное окно для просмотра
- Анимация при наведении
- Категории фото

**Редактируемые параметры в `Gallery.tsx`:**
```tsx
const photos = [
  {
    id: 1,
    src: 'ссылка_на_фото.jpg',
    alt: 'Описание',
    category: 'Портреты'
  },
  // ...добавьте больше фото
];
```

### 3. **About** - О фотографе
- Фото и биография
- Статистика (количество проектов, опыт и т.д.)
- Кнопка для контакта

**Редактируемые параметры в `About.tsx`:**
```tsx
<About 
  title="Обо мне"
  description="Ваш текст здесь..."
  stats={[
    { label: 'Проектов', value: '+500' },
    // ...добавьте свои статистики
  ]}
/>
```

### 4. **Contact** - Форма контакта
- Форма с полями для имени, почты, темы и сообщения
- Контактная информация
- Ссылки на социальные сети
- Футер

**Редактируемые параметры в `Contact.tsx`:**
```tsx
<Contact 
  title="Свяжитесь со мной"
  subtitle="Давайте создадим что-то прекрасное вместе"
  email="your@email.com"
  phone="+373 69 123 456"
  social={[
    { name: 'Instagram', url: 'https://instagram.com/you' },
    // ...добавьте другие соцсети
  ]}
/>
```

## 🚀 Как начать

1. **Установить зависимости:**
```bash
npm install
```

2. **Запустить в режиме разработки:**
```bash
npm start
```

3. **Собрать для продакшена:**
```bash
npm build
```

## 🎨 Кастомизация

### Редактировать фото
- Откройте нужный компонент (например, `Gallery.tsx`)
- Замените URL фото в свойстве `src`

### Изменить текст
- Найдите нужный компонент
- Измените значение в `props`

### Изменить стили
- Каждый компонент имеет свой файл CSS (например, `Hero.css`)
- Отредактируйте стили прямо в этом файле
- **Важно:** CSS изолирован для каждого компонента, поэтому изменения в одном файле не повлияют на другие

### Изменить цвета
- Все компоненты используют тёмный стиль (#0a0a0a, #1a1a1a)
- Найдите `background-color`, `color` и измените значения

### Изменить шрифты
- Отредактируйте `font-size`, `font-weight`, `letter-spacing` в CSS файлах

## 📱 Адаптивность

Все компоненты полностью адаптированы для:
- 📱 Мобильных устройств (480px и меньше)
- 📱 Планшетов (768px и меньше)
- 💻 Ноутбуков
- 🖥️ Больших экранов

## 💡 Советы для новых разработчиков

1. **Каждый компонент в отдельной папке** - так проще ориентироваться
2. **CSS в отдельном файле** - так не смешивается с логикой
3. **Props для кастомизации** - позволяет переиспользовать компоненты
4. **Понятные имена классов** - используется BEM методология (`component__element`)
5. **Комментарии в коде** - помогают понять, что делает каждый блок

## 🛠️ Технологии

- **React 19** - для создания интерфейса
- **TypeScript** - для типизации кода
- **CSS3** - для стилизации и анимаций
- **HTML5** - семантичный markup

## 📝 Лицензия

Свободно использовать и редактировать для своих проектов.

---

**Создано с ❤️ для фотографов**

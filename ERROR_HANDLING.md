# ✅ Автоматическое перенаправление при ошибках

## 🎯 Что было изменено:

### **1. App.tsx** - Логика перенаправления
```tsx
// Теперь вся логика загрузки в отдельном компоненте AppContent
function AppContent() {
  const navigate = useNavigate();
  // ...
  
  // При ошибке автоматически перенаправляет на /error через 2 секунды
  setTimeout(() => navigate('/error'), 2000);
}
```

### **2. NotFound.tsx** - Разные сообщения об ошибках
```tsx
const isErrorPage = location.pathname === '/error';

// Если это страница /error - показывает ошибку сервера
// Если это неизвестный маршрут - показывает "Страница не найдена"
```

### **3. Маршруты**
```tsx
<Route path="/" element={<AppContent />} />         {/* Главная */}
<Route path="/error" element={<NotFound />} />      {/* Ошибка сервера */}
<Route path="*" element={<NotFound />} />           {/* 404 для неизвестных маршрутов */}
```

## 🔄 Процесс работы:

### Сценарий 1: Успех ✅
```
1. Пользователь открывает http://localhost:3000/
2. Приложение загружается (loading спиннер)
3. Загружаются данные из Strapi
4. Отображается галерея фотографий
```

### Сценарий 2: Ошибка подключения ❌
```
1. Пользователь открывает http://localhost:3000/
2. Приложение пытается подключиться к Strapi
3. Показывает сообщение об ошибке (2 секунды)
4. Автоматически перенаправляет на /error
5. Отображается страница 404 с сообщением об ошибке
```

### Сценарий 3: Неизвестный маршрут 🔗
```
1. Пользователь открывает http://localhost:3000/unknown
2. React Router не найден маршрут
3. Отображается страница 404 с сообщением "Страница не найдена"
```

## 📊 Сообщения об ошибках:

| Маршрут | Сообщение | Причина |
|---------|-----------|---------|
| `/` (успех) | - | Галерея загружена |
| `/error` | "Ошибка подключения к серверу" | Strapi недоступен |
| `/unknown` | "Страница не найдена" | Маршрут не существует |
| `/*` | "Страница не найдена" | Catch-all для всех остальных |

## 🧪 Тестирование:

### Тест 1: Проверка перенаправления при ошибке
```bash
# 1. Убедитесь, что Strapi НЕ запущен
# 2. Откройте http://localhost:3000/
# 3. Увидите ошибку, а затем перенаправление на 404 страницу
```

### Тест 2: Проверка 404 страницы
```bash
# 1. Откройте http://localhost:3000/unknown-page
# 2. Увидите 404 страницу с котом и сообщением
```

### Тест 3: Нормальная работа
```bash
# 1. Запустите Strapi: npm run develop
# 2. Откройте http://localhost:3000/
# 3. Увидите галерею фотографий
```

## 📝 Логика в коде:

### App.tsx
```typescript
// Перенаправление при ошибке
if (galleries && galleries.length > 0) {
  setGallery(galleries[0]);
} else {
  setError('Галереи не найдены на сервере');
  // Перенаправляем на /error через 2 секунды
  setTimeout(() => navigate('/error'), 2000);
}
```

### NotFound.tsx
```typescript
// Определение типа ошибки
const isErrorPage = location.pathname === '/error';

// Разные сообщения
if (isErrorPage) {
  return 'Ошибка подключения к серверу';
} else {
  return 'Страница не найдена';
}
```

## ⚙️ Настройки:

### Время задержки перед перенаправлением
В `App.tsx` строка ~43:
```typescript
setTimeout(() => navigate('/error'), 2000); // 2 секунды
```

Чтобы изменить на 3 секунды:
```typescript
setTimeout(() => navigate('/error'), 3000);
```

---

**✅ Всё готово! При ошибке подключения пользователь будет автоматически перенаправлен на красивую 404 страницу с котом! 🐱**

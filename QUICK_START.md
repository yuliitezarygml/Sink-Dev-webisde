# 🎯 КРАТКАЯ ШПАРГАЛКА

## Структура файлов проекта

```
📁 src/
├── 📁 components/
│   ├── 📁 Hero/
│   │   ├── Hero.tsx          ← Компонент (код)
│   │   └── Hero.css          ← Стили (оформление)
│   ├── 📁 Gallery/
│   │   ├── Gallery.tsx
│   │   └── Gallery.css
│   ├── 📁 About/
│   │   ├── About.tsx
│   │   └── About.css
│   └── 📁 Contact/
│       ├── Contact.tsx
│       └── Contact.css
├── App.tsx                   ← Главный файл приложения
├── App.css                   ← Глобальные стили
└── index.tsx                 ← Точка входа
```

---

## 🔧 Как редактировать

### 1️⃣ Изменить текст
**Файл:** `src/App.tsx` или соответствующий компонент
```tsx
<Hero 
  names={{ first: 'Ваше имя', second: 'и имя партнера' }}
/>
```

### 2️⃣ Изменить фото
**Файл:** `src/components/Gallery/Gallery.tsx`
```tsx
src: 'https://ваша-ссылка-на-фото.jpg'
```

### 3️⃣ Изменить стили (цвета, размеры, шрифты)
**Файл:** `src/components/[ComponentName]/[ComponentName].css`
```css
.hero__title {
  font-size: 72px;      ← размер текста
  color: white;         ← цвет текста
  background: #000;     ← цвет фона
}
```

### 4️⃣ Изменить контакты
**Файл:** `src/components/Contact/Contact.tsx`
```tsx
<Contact 
  email="ваша@почта.com"
  phone="+123 45 678"
/>
```

---

## 📱 Структура страницы (сверху вниз)

```
┌─────────────────────────────────────┐
│           HERO SECTION              │  ← Главный экран с фото
├─────────────────────────────────────┤
│         GALLERY SECTION             │  ← Галерея фото
├─────────────────────────────────────┤
│          ABOUT SECTION              │  ← Информация о вас
├─────────────────────────────────────┤
│        CONTACT SECTION              │  ← Форма контакта
└─────────────────────────────────────┘
```

---

## 💻 Команды для запуска

```bash
# Запустить в режиме разработки (с горячей перезагрузкой)
npm start

# Собрать для продакшена
npm build

# Запустить тесты
npm test
```

---

## 🎨 Цветовая палитра (текущая - тёмный стиль)

| Элемент | Цвет | Код |
|---------|------|-----|
| Фон | Очень тёмный | `#0a0a0a` |
| Фон секций | Тёмный | `#1a1a1a` |
| Текст | Белый | `white` |
| Акцент | Белый с прозрачностью | `rgba(255,255,255,0.8)` |

---

## 📋 Компоненты и что они делают

| Компонент | Файл | Для чего |
|-----------|------|---------|
| **Hero** | `Hero/Hero.tsx` | Главный экран с именами и фото |
| **Gallery** | `Gallery/Gallery.tsx` | Сетка фото с модальным окном |
| **About** | `About/About.tsx` | Информация о фотографе и статистика |
| **Contact** | `Contact/Contact.tsx` | Форма для связи и контакты |

---

## ⚡ Важные советы

✅ **Делайте:**
- Редактируйте значения `props` в компонентах (например, `names`, `email`)
- Меняйте CSS в файлах компонентов (`*.css`)
- Сохраняйте структуру папок
- Используйте нормальные ссылки на фото

❌ **Не делайте:**
- Не удаляйте папки компонентов
- Не трогайте `node_modules` папку
- Не меняйте имена файлов
- Не смешивайте CSS разных компонентов

---

## 🆘 Если что-то сломалось

1. Откройте консоль браузера (F12)
2. Посмотрите на ошибку
3. Проверьте, что все кавычки на месте
4. Убедитесь, что ссылки на фото работают
5. Сохраните файл (Ctrl+S)
6. Обновите браузер (F5)

---

## 📚 Полезные файлы в проекте

- **GUIDE.md** - полное руководство
- **CUSTOMIZATION_EXAMPLES.js** - примеры кода
- **QUICK_START.md** - этот файл (шпаргалка)

---

**Удачи! 🚀**

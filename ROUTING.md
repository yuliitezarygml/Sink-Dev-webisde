# 📍 Маршруты приложения (Routes)

## Доступные страницы:

### 🏠 **Главная страница** → `/`
- **Компонент**: `Gallery`
- **Описание**: Галерея фотографий с Hero секцией
- **Содержит**: Фотографии, информацию о фотографе, меню контактов

### ❌ **Страница 404** → `*` (все остальные маршруты)
- **Компонент**: `NotFound`
- **Описание**: Страница для несуществующих маршрутов
- **Содержит**: Большая цифра 404, анимация кота, кнопка "Вернуться на главную"

## Примеры навигации:

```
✅ http://localhost:3000/              → Главная страница (галерея)
✅ http://localhost:3000/gallery       → Страница 404 (редирект)
✅ http://localhost:3000/unknown       → Страница 404 (редирект)
✅ http://localhost:3000/anything      → Страница 404 (редирект)
```

## Структура маршрутизации:

```tsx
<BrowserRouter>
  <Routes>
    {/* Главная страница */}
    <Route path="/" element={<Gallery />} />
    
    {/* Catch-all для 404 */}
    <Route path="*" element={<NotFound />} />
  </Routes>
</BrowserRouter>
```

## Как добавить новый маршрут:

1. Создайте новый компонент в `src/components/`
2. Добавьте импорт в `App.tsx`
3. Добавьте новый `<Route>` перед catch-all маршрутом `path="*"`

**Пример:**
```tsx
<Route path="/about" element={<About />} />
<Route path="/portfolio" element={<Portfolio />} />
<Route path="*" element={<NotFound />} />
```

## Компоненты с маршрутизацией:

- ✅ **ContactMenu** - плавающее меню контактов (доступно на всех страницах)
- ✅ **Gallery** - главная галерея фотографий
- ✅ **NotFound** - страница ошибки 404

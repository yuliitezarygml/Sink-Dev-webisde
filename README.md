# 📸 Sink-Dev Photography Portfolio - Полная Документация

## 📑 Содержание
1. [Общее описание](#общее-описание)
2. [Структура проекта](#структура-проекта)
3. [Установка и запуск](#установка-и-запуск)
4. [Используемые технологии](#используемые-технологии)
5. [Стиль и оформление](#стиль-и-оформление)
6. [JSON Конфигурация](#json-конфигурация)
7. [Компоненты](#компоненты)
8. [Модули CSS](#модули-css)
9. [Анимации и эффекты](#анимации-и-эффекты)
10. [Адаптивный дизайн](#адаптивный-дизайн)
11. [Развертывание](#развертывание)
12. [EmailJS Интеграция](#emailjs-интеграция-новое)
13. [GitHub Pages Развертывание](#github-pages-развертывание-новое)

## 📚 Дополнительная документация

- 📧 [`EMAILJS_QUICK_START.md`](./EMAILJS_QUICK_START.md) - EmailJS быстрый старт
- 📖 [`EMAILJS_SETUP.md`](./EMAILJS_SETUP.md) - EmailJS полная инструкция
- 🚀 [`GITHUB_PAGES_QUICK_START.md`](./GITHUB_PAGES_QUICK_START.md) - GitHub Pages быстрый старт
- ⚡ [`GITHUB_PAGES_STEP_BY_STEP.md`](./GITHUB_PAGES_STEP_BY_STEP.md) - Пошаговое руководство
- 📋 [`QUICK_REFERENCE.md`](./QUICK_REFERENCE.md) - Краткий справочник
- 📊 [`FINAL_SUMMARY.md`](./FINAL_SUMMARY.md) - Итоговый отчёт

---

## 🎨 Общее описание

**Sink-Dev Photography Portfolio** — это современный веб-сайт портфолио фотографа, созданный на React с использованием TypeScript. Сайт имеет киберпанк эстетику с неоновыми эффектами, плавающими UI элементами и полностью управляется через JSON конфигурацию.

### Ключевые особенности:
- ✨ Киберпанк дизайн с glitch эффектами и RGB сдвигом
- 🎯 Модульная архитектура компонентов
- 📱 100% адаптивный дизайн (мобиль, планшет, ПК)
- 🎨 Полное управление контентом через JSON
- 🖼️ Галерея с 50+ фотографиями
- 🎪 Плавающее меню с модальными окнами
- ⚡ Сложные CSS анимации и переходы
- 📧 **НОВОЕ**: Интеграция EmailJS для отправки писем
- 🚀 **НОВОЕ**: Автоматическая публикация на GitHub Pages через GitHub Actions

---

## 📁 Структура проекта

```
sink-dev-webisde/
├── public/
│   ├── index.html                 # HTML точка входа
│   ├── manifest.json              # PWA манифест
│   ├── robots.txt                 # SEO инструкции
│   ├── data/
│   │   └── data.json              # Главный конфиг всего контента
│   └── photos/                    # Директория с фотографиями (50+ JPG)
│       ├── IMG_0006.jpg
│       ├── IMG_0015.jpg
│       └── ... (всего 52 изображения)
│
├── src/
│   ├── index.tsx                  # React точка входа
│   ├── App.tsx                    # Корневой компонент с роутингом
│   ├── App.css                    # Глобальные стили
│   ├── index.css                  # Базовые стили
│   ├── react-app-env.d.ts         # Типы Create React App
│   ├── reportWebVitals.ts         # Производительность метрики
│   ├── setupTests.ts              # Jest конфигурация
│   │
│   └── components/
│       ├── Hero/                  # Секция героя
│       │   ├── Hero.tsx
│       │   └── Hero.css
│       │
│       ├── Gallery/               # Галерея фотографий
│       │   ├── Gallery.tsx
│       │   └── Gallery.css
│       │
│       ├── FloatingMenu/          # Плавающее меню (гамбургер)
│       │   ├── FloatingMenu.tsx
│       │   └── FloatingMenu.css
│       │
│       ├── FloatingAbout/         # Модаль "Обо мне"
│       │   ├── FloatingAbout.tsx
│       │   └── FloatingAbout.css
│       │
│       ├── FloatingContact/       # Модаль "Контакты"
│       │   ├── FloatingContact.tsx
│       │   └── FloatingContact.css
│       │
│       ├── PhotoGallery.tsx       # Компонент галереи (старая версия)
│       └── PhotoGallery.css
│
├── package.json                   # Зависимости и скрипты
├── tsconfig.json                  # TypeScript конфигурация
├── README.md                      # Базовый README
├── DOCUMENTATION.md               # Эта документация
└── .gitignore

```

---

## 🚀 Установка и запуск

### Требования:
- Node.js 16+ 
- npm 7+ или yarn

### Установка зависимостей:
```bash
npm install
```

### Запуск в режиме разработки:
```bash
npm start
```
Открыть [http://localhost:3000](http://localhost:3000)

### Сборка для production:
```bash
npm run build
```

### Запуск тестов:
```bash
npm test
```

---

## 🛠️ Используемые технологии

### Core Framework
- **React 19.2.0** — библиотека для построения UI
- **React DOM 19.2.0** — отрисовка React компонентов в браузер
- **TypeScript 4.9.5** — строгая типизация JavaScript

### Build & Dev Tools
- **Create React App 5.0.1** — конфигурация webpack, babel, eslint
- **React Scripts 5.0.1** — скрипты для dev/build

### Testing
- **Jest** — фреймворк тестирования (встроен в CRA)
- **React Testing Library** — тестирование React компонентов
- **@testing-library/jest-dom** — матчеры для DOM
- **@testing-library/user-event** — симуляция действий пользователя

### Type Definitions
- **@types/react 18.3.1** — типы React
- **@types/react-dom 18.3.0** — типы React DOM
- **@types/jest 29.5.13** — типы Jest
- **@types/node 20.17.6** — типы Node.js

### CSS & Styling
- **CSS3** с @keyframes анимациями
- **BEM методология** (Block Element Modifier)
- **CSS переменные** для тем и цветов
- **Flex Layout** для позиционирования
- **Grid Layout** для статистики

---

## 🎨 Стиль и оформление

### Цветовая схема (Киберпанк)

```css
/* Основные цвета */
--primary-blue: #667eea          /* Электрический синий */
--secondary-pink: #f5576c        /* Неоновый розовый */
--dark-bg: #0a0a0a               /* Черный фон */
--dark-secondary: #1a1a1a        /* Серый фон */
--dark-tertiary: #2a2a2a         /* Светлый серый */
--text-primary: #ffffff          /* Белый текст */
--text-secondary: #999999        /* Серый текст */
--neon-glow: rgba(102, 126, 234, 0.4)  /* Неоновое свечение */
```

### Типография

```css
/* Шрифты */
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
            'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
            sans-serif;

/* Заголовки */
h1: font-size 48px, font-weight 700, letter-spacing 2px
h2: font-size 36px, font-weight 700
h3: font-size 24px, font-weight 600

/* Текст */
body: font-size 16px, line-height 1.6, letter-spacing 0.5px
p: font-size 16px, color #999
small: font-size 12px
```

### Тени и свечение

```css
/* Неоновое свечение */
box-shadow: 0 0 20px rgba(102, 126, 234, 0.4),
            inset 0 0 15px rgba(102, 126, 234, 0.1),
            0 0 40px rgba(102, 126, 234, 0.3);

/* Текстовое свечение */
text-shadow: 0 0 10px rgba(102, 126, 234, 0.8),
             0 0 20px rgba(245, 87, 108, 0.4);

/* Обычная тень */
box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
```

---

## 📋 JSON Конфигурация

### Файл: `public/data/data.json`

```json
{
  "hero": {
    "title": "Профессиональный фотограф",
    "subtitle": "Запечатлевая моменты, создавая воспоминания",
    "cta": "Посмотреть работы",
    "backgroundImage": "/photos/IMG_0183.jpg"
  },

  "about": {
    "title": "Обо мне",
    "description": "Профессиональный фотограф с многолетним опытом...",
    "profileImage": "/photos/IMG_0183.jpg",
    "stats": [
      { "label": "Лет опыта", "value": "10+" },
      { "label": "Проектов", "value": "500+" },
      { "label": "Довольных клиентов", "value": "1000+" }
    ]
  },

  "contact": {
    "title": "Свяжитесь со мной",
    "email": "your@email.com",
    "phone": "+1 (555) 123-4567",
    "location": "Город, Страна",
    "socialLinks": [
      { "platform": "Instagram", "url": "https://instagram.com/..." },
      { "platform": "Email", "url": "mailto:your@email.com" }
    ]
  },

  "photos": [
    { "id": 1, "url": "/photos/IMG_0006.jpg", "title": "Пейзаж" },
    { "id": 2, "url": "/photos/IMG_0015.jpg", "title": "Портрет" },
    /* ... 50+ фотографий */
  ]
}
```

### Редактирование контента

Для изменения любого контента на сайте достаточно отредактировать `data.json`:
1. Откройте `public/data/data.json`
2. Измените нужные значения
3. Сохраните файл
4. Страница автоматически обновится (при npm start)

---

## 🧩 Компоненты

### 1. Hero Component (`src/components/Hero/Hero.tsx`)

**Назначение**: Секция-герой с главным заголовком

**Props**:
- `title`: string — главный заголовок
- `subtitle`: string — подзаголовок
- `backgroundImage`: string — URL фонового изображения

**Функционал**:
- Отображает фоновое изображение
- Выводит анимированный заголовок
- Кнопка "Посмотреть работы" (скроллит к галерее)

---

### 2. Gallery Component (`src/components/Gallery/Gallery.tsx`)

**Назначение**: Сетка фотографий с отзывчивым дизайном

**Props**:
- `photos`: Array — массив объектов фотографий
  ```typescript
  interface Photo {
    id: number;
    url: string;
    title?: string;
  }
  ```

**Функционал**:
- Отображает фотографии в grid (3 колонки на ПК)
- Масонный макет (разные размеры)
- Hover эффекты и масштабирование
- Полностью адаптивна

---

### 3. FloatingMenu Component (`src/components/FloatingMenu/FloatingMenu.tsx`)

**Назначение**: Плавающее меню с гамбургер иконом

**Props**:
- `onAboutClick`: () => void — callback для открытия About
- `onContactClick`: () => void — callback для открытия Contact

**Состояние**:
- `isOpen`: boolean — раскрыто ли меню

**Функционал**:
- Анимированный гамбургер икон (3 линии)
- При клике раскрывается меню с 2 кнопками
- RGB glitch эффекты на кнопках
- Киберпанк anимации (2-2.5 сек цикл)

---

### 4. FloatingAbout Component (`src/components/FloatingAbout/FloatingAbout.tsx`)

**Назначение**: Модальное окно с информацией о фотографе

**Props**:
- `isOpen`: boolean — открыто ли окно
- `onClose`: () => void — callback для закрытия

**Состояние**:
```typescript
interface AboutData {
  title: string;
  description: string;
  profileImage: string;
  stats: Array<{ label: string; value: string }>;
}
```

**Функционал**:
- Загружает данные из `data.json` при открытии
- Отображает профиль фотографа (250x250px)
- Показывает статистику в 3 колонки
- Кнопка "Написать мне"

---

### 5. FloatingContact Component (`src/components/FloatingContact/FloatingContact.tsx`)

**Назначение**: Модальное окно для контактной информации

**Props**:
- `isOpen`: boolean — открыто ли окно
- `onClose`: () => void — callback для закрытия

**Функционал**:
- Отображает email, телефон, локацию
- Социальные ссылки (Instagram, Email)
- Контактная форма с отправкой
- Валидация полей

---

## 🎨 Модули CSS

### Global Styles (`src/App.css`)

```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  background: #0a0a0a;
  color: #ffffff;
  font-family: system-ui, -apple-system, sans-serif;
  line-height: 1.6;
}

html {
  scroll-behavior: smooth;
}
```

### BEM Методология

Все компоненты используют BEM (Block Element Modifier):

```css
/* Block */
.floating-about { }

/* Element */
.floating-about__modal { }
.floating-about__image { }
.floating-about__title { }

/* Modifier */
.floating-about__button--primary { }
.floating-about__button--secondary { }
.floating-about__content--active { }
```

### Компонентные стили

- **Hero.css** — стили секции героя, фон, анимации
- **Gallery.css** — grid, hover эффекты, адаптивность
- **FloatingMenu.css** — гамбургер анимация, glitch эффекты
- **FloatingAbout.css** — модальное окно, профиль, статистика
- **FloatingContact.css** — форма, контакты, ссылки

---

## ✨ Анимации и эффекты

### 1. Glitch Flicker (Главная кнопка меню)

```css
@keyframes glitch-flicker {
  0%, 100% { 
    transform: translate(0) scaleX(1);
    text-shadow: none;
  }
  25% { 
    transform: translate(-2px) scaleX(1.05);
    text-shadow: -3px 0 #667eea, 3px 0 #f5576c;
  }
  50% { 
    transform: translate(0) scaleX(1);
    text-shadow: none;
  }
  75% { 
    transform: translate(2px) scaleX(0.95);
    text-shadow: 2px 0 #f5576c, -2px 0 #667eea;
  }
}
```

Длительность: 2 сек, бесконечно

### 2. RGB Color Shift (Кнопки меню)

```css
@keyframes glitch-about {
  0%, 100% {
    color: #667eea;
    text-shadow: 0 0 10px rgba(102, 126, 234, 0.8);
  }
  50% {
    color: #f5576c;
    text-shadow: 0 0 15px rgba(245, 87, 108, 0.8);
  }
}
```

Длительность: 2.5 сек, бесконечно

### 3. Image Glitch (Профиль фотографа)

```css
@keyframes image-glitch {
  0%, 100% {
    box-shadow: 0 0 25px rgba(102, 126, 234, 0.4);
    filter: brightness(1) contrast(1);
  }
  25% {
    box-shadow: -3px 0 rgba(102, 126, 234, 0.6), 3px 0 rgba(245, 87, 108, 0.6);
    filter: brightness(1.1) contrast(1.2);
  }
  50% {
    box-shadow: 0 0 40px rgba(102, 126, 234, 0.6);
    filter: brightness(1) contrast(1.1);
  }
  75% {
    box-shadow: 2px 0 rgba(245, 87, 108, 0.6), -2px 0 rgba(102, 126, 234, 0.6);
    filter: brightness(1.05) contrast(1.15);
  }
}
```

Длительность: 3 сек, бесконечно

### 4. Fade In (Модальные окна)

```css
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}
```

Длительность: 0.3 сек

### 5. Slide Up (Появление модала)

```css
@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(50px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

Длительность: 0.4 сек

### 6. Hover Scale (Интерактивные элементы)

```css
.element:hover {
  transform: scale(1.05);
  transition: all 0.3s ease;
}
```

---

## 📱 Адаптивный дизайн

### Breakpoints

| Устройство | Ширина | Изменения |
|-----------|--------|----------|
| Mobile | ≤480px | 1 колонка, меньше шрифты |
| Tablet | 481-768px | 2 колонки, средний размер |
| Desktop | ≥769px | 3 колонки, полный размер |
| Large | ≥1200px | 4 колонки, максимум |

### Адаптивные элементы

#### Hero
```css
@media (max-width: 768px) {
  h1 { font-size: 28px; }
}

@media (max-width: 480px) {
  h1 { font-size: 20px; }
}
```

#### Gallery
```css
@media (max-width: 768px) {
  grid-template-columns: repeat(2, 1fr);
}

@media (max-width: 480px) {
  grid-template-columns: 1fr;
}
```

#### Floating Menu
```css
@media (max-width: 768px) {
  width: 50px;
  height: 50px;
  bottom: 20px;
  right: 20px;
}
```

#### About Modal
```css
@media (max-width: 768px) {
  .floating-about__image { width: 200px; height: 200px; }
}

@media (max-width: 480px) {
  .floating-about__image { width: 150px; height: 150px; }
}
```

---

## 🚀 Развертывание

### Локальная разработка

1. Клонировать репозиторий:
```bash
git clone https://github.com/yuliitezarygml/Sink-Dev-webisde.git
cd Sink-Dev-webisde
```

2. Установить зависимости:
```bash
npm install
```

3. Запустить dev сервер:
```bash
npm start
```

4. Открыть http://localhost:3000

### Build для Production

```bash
npm run build
```

Создаст папку `build/` с оптимизированными файлами.

### Развертывание на Vercel

1. Подключить GitHub репозиторий к Vercel
2. Vercel автоматически:
   - Обнаружит Create React App
   - Установит зависимости
   - Создаст build
   - Развернет на CDN

### Развертывание на Netlify

```bash
npm run build
# Перетащить папку build на netlify.com
```

### Развертывание на GitHub Pages

1. Добавить в `package.json`:
```json
{
  "homepage": "https://yuliitezarygml.github.io/Sink-Dev-webisde"
}
```

2. Установить gh-pages:
```bash
npm install --save-dev gh-pages
```

3. Добавить скрипты в `package.json`:
```json
{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
```

4. Развернуть:
```bash
npm run deploy
```

---

## 📦 Размер сборки

- **Production bundle**: ~150-200KB (minified)
- **JavaScript**: ~120KB
- **CSS**: ~30KB
- **Images**: ~2-5MB (зависит от количества фото)

---

## 🔧 Расширение проекта

### Добавление новой фотографии

1. Поместить JPG файл в `public/photos/`
2. Добавить запись в `public/data/data.json`:
```json
{
  "id": 53,
  "url": "/photos/NEW_PHOTO.jpg",
  "title": "Название фото"
}
```

### Добавление нового компонента

1. Создать папку: `src/components/NewComponent/`
2. Создать файлы:
   - `NewComponent.tsx` (JSX логика)
   - `NewComponent.css` (стили)
3. Импортировать в `App.tsx`

### Изменение цветов

Обновить переменные в CSS:
```css
:root {
  --primary-blue: #667eea;
  --secondary-pink: #f5576c;
  /* ... */
}
```

---

## 📞 Поддержка

- GitHub Issues: https://github.com/yuliitezarygml/Sink-Dev-webisde/issues
- Email: yuliitezarywork@gmail.com

---

## 📄 Лицензия

MIT License - свободен для личного и коммерческого использования.

---

## 📧 EmailJS Интеграция (НОВОЕ)

Проект полностью интегрирован с **EmailJS** для отправки писем через контактную форму.

### Быстрый старт:
1. Зарегистрируйтесь на https://www.emailjs.com/
2. Получите `publicKey`, `serviceId`, `templateId`
3. Обновите `public/data/data.json`:
```json
{
  "emailjs": {
    "publicKey": "YOUR_PUBLIC_KEY",
    "serviceId": "YOUR_SERVICE_ID",
    "templateId": "YOUR_TEMPLATE_ID",
    "enabled": true
  }
}
```

📖 **Полная документация**: [`EMAILJS_QUICK_START.md`](./EMAILJS_QUICK_START.md)

---

## 🚀 GitHub Pages Развертывание (НОВОЕ)

Проект готов к автоматической публикации на **GitHub Pages** с использованием **GitHub Actions**.

### Быстрый старт:
```bash
git init
git add .
git commit -m "Initial commit"
git push -u origin main
```

Затем включите Pages в Settings → Pages → gh-pages branch.

**Сайт автоматически собирается и публикуется при каждом push!**

📖 **Полная документация**: [`GITHUB_PAGES_QUICK_START.md`](./GITHUB_PAGES_QUICK_START.md)

⚡ **Пошаговое руководство**: [`GITHUB_PAGES_STEP_BY_STEP.md`](./GITHUB_PAGES_STEP_BY_STEP.md)

📋 **Справочник**: [`QUICK_REFERENCE.md`](./QUICK_REFERENCE.md)

---

**Дата документации**: 9 ноября 2025 г.
**Версия проекта**: 1.0.0 + EmailJS + GitHub Pages CI/CD
**React версия**: 19.2.0
